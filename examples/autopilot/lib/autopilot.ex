defmodule Autopilot do
  @moduledoc """
  Browser automation agent combining Bibbidi + Sagents.

  Usage:
      iex -S mix
      iex> Autopilot.CLI.start()
  """

  def start do
    Autopilot.CLI.start()
  end
end
