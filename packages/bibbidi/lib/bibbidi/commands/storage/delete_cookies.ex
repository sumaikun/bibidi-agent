# Generated by mix bibbidi.gen — do not edit manually
defmodule Bibbidi.Commands.Storage.DeleteCookies do
  @moduledoc """
  Command struct for `storage.deleteCookies`.
  """

  defstruct [:filter, :partition]

  defimpl Bibbidi.Encodable do
    def method(_), do: "storage.deleteCookies"

    def params(cmd) do
      optional = [
        {:filter, cmd.filter},
        {:partition, cmd.partition}
      ]

      Enum.reduce(optional, %{}, fn
        {_key, nil}, acc -> acc
        {key, value}, acc -> Map.put(acc, key, value)
      end)
    end
  end
end
